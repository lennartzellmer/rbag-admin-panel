meta {
  name: Cancel
  type: http
  seq: 3
}

post {
  url: {{base_url}}/api/events/:event-id/cancel
  body: json
  auth: none
}

params:path {
  event-id: {{eventId}}
}

body:json {
  {
    "categoryId": "{{categoryId}}"
  }
}
