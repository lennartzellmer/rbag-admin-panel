meta {
  name: Get paginated
  type: http
  seq: 3
}

get {
  url: {{base_url}}/api/veranstaltung-kategorie?offset=0&limit=100
  body: json
  auth: inherit
}

params:query {
  offset: 0
  limit: 100
}
