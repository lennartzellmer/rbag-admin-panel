meta {
  name: Get users
  type: http
  seq: 2
}

get {
  url: {{base_url}}/api/admin/user?offset=0&limit=10
  body: none
  auth: inherit
}

params:query {
  offset: 0
  limit: 10
}

settings {
  encodeUrl: true
  timeout: 0
}
